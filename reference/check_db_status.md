# Check whether databases are currently online

Attempt to access all presently supported databases and report if they
were accessible.

## Usage

``` r
check_db_status()
```

## Value

TRUE if all DB checks pass, else FALSE

## Author

Francis Windram

## Examples

``` r
if (FALSE) { # interactive()
  check_db_status()
}
```
