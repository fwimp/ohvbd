# collapse a list of character strings to a JS space-separated single string

collapse a list of character strings to a JS space-separated single
string

## Usage

``` r
space_collapse(v)
```

## Arguments

- v:

  a vector to format.

## Value

collapsed string.
