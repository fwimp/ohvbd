# Search VecDyn by keyword

Retrieve the IDs for any VecDyn datasets matching the given keywords

## Usage

``` r
search_vd(keywords, basereq = NA)
```

## Arguments

- keywords:

  either a string of search terms separated by spaces, or a vector of
  keywords.

- basereq:

  an [httr2 request](https://httr2.r-lib.org/reference/request.html)
  object, as generated by
  [`vb_basereq()`](https://ohvbd.vbdhub.org/reference/vb_basereq.md). If
  `NA`, uses the default request.

## Value

An `ohvbd.ids` vector of VecDyn dataset IDs.

## Author

Francis Windram

## Examples

``` r
if (FALSE) { # interactive()
search_vd("Aedes aegypti")

search_vd(c("Aedes", "aegypti"))
}
```
